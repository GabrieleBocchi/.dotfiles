# Other sources
source "$DOTFILES"/zsh/oh-my-zsh.zsh
source "$DOTFILES"/zsh/aliases.zsh

# Opts
setopt extendedglob
setopt globdots
zstyle ':completion:*' special-dirs false

# Tool inits (PATH, pyenv, bun, etc.) — alphabetical order
for f in $DOTFILES/zsh/init/*.zsh(N); do source "$f"; done
